server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:49153/ecommerce
    username: postgres
    password: postgrespw
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update


  jackson:
    default-property-inclusion: non_null
